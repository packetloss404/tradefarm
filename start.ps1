# TradeFarm — launches backend + dashboard + stream Tauri in one terminal.
# Right-click → Run with PowerShell, or invoke `.\start.ps1` from a PS prompt.
Set-Location -LiteralPath $PSScriptRoot
npm run dev
